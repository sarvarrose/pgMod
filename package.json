{
  "name": "pgMod",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "lint": "eslint .",
    "prepare": "ncc build index.js -o dist",
    "watch": "ncc build index.js -w -o dist",
    "test": "jest",
    "all": "npm run lint && npm run prepare && npm run test"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1",
    "eslint": "^8.37.0"
  },
  "license": "MIT"
}
